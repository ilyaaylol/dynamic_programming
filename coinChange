def coinChange(coins, amount):
    maxInt = amount + 1
    dp = [maxInt] * maxInt
    dp[0] = 0 # для суммы ноль нужно ноль монет
    for i in range(1, amount + 1):
        for coin in coins:
            if coin <= i:
                dp[i] = min(dp[i], dp[i - coin] + 1)

    if dp[amount] == maxInt:
        return -1

    return dp[amount]

print(coinChange([4,3], 6))
